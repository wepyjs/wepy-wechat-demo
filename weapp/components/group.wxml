
<view class="list">
    <view wx:for="{{ list }}" wx:for-index="index" wx:for-item="item" wx:key="index">
        <item bind_init="_initComponent" item="{{ item }}" index="{{ index }}"></item>
    </view>
</view>
