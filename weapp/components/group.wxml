
<view class="list">
    <view wx:for="{{ list }}" wx:for-index="index" wx:for-item="item" wx:key="index">
        <item item="{{ item }}" index="{{ index }}" bind_init="_initComponent"></item>
    </view>
</view>
